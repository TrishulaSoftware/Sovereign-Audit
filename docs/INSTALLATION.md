# [INSTALLATION] - SOVEREIGN-AUDIT NODE

### PREREQUISITES
- Python 3.10+
- Access to the Trishula Sovereign Vault (via standard SSH identity `ascension`)

### SETUP
1. Clone the repository:
   ```bash
   git clone git@ascension:trishulasoftware/Sovereign-Audit.git
   ```
2. No external dependencies required (Standard Library only for MC/DC Determinism).

### VERIFICATION
Run the auditor against the local README to verify the installation:
```bash
python main.py README.md
```
